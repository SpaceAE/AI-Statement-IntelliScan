# requirements.txt
numpy>=1.23,<2.0
pandas>=2.0
scikit-learn>=1.3
openpyxl>=3.1
jupyterlab>=4
ipykernel>=6.25

# เลือกอย่างใดอย่างหนึ่งตามเครื่อง:
# tensorflow==2.15.*        # สำหรับ Intel Mac / Windows
tensorflow-macos==2.15.* # สำหรับ Apple Silicon (M1/M2/M3/M4)
# tensorflow-metal==1.*    # (ถ้าเป็น Apple Silicon และอยากใช้ GPU Metal)
